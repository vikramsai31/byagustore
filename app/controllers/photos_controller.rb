class PhotosController < ApplicationController
	def new
		#@product = Product.find(params[:product_id])
		#@photo = @product.photos.build
	end
	
	def create
		#@product = Product.find(params[:product_id])
		#@photo = @product.photos.create(params[:photo])
		 #redirect_to product_path(@product)
	end

end
